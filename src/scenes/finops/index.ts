import type { Scene } from '@graphlearning/flow'

// Scenes for the `finops` course — roughly one solid scene per section, authored FRESH.
// Empty until this course's first slice lands.
export const finopsScenes: Scene[] = []
